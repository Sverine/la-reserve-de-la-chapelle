{% extends 'base.html.twig' %}

{% block title %}BookLoan{% endblock %}

{% block body %}
    <h1>BookLoan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_loan.id }}</td>
            </tr>
            <tr>
                <th>Date_loan</th>
                <td>{{ book_loan.dateLoan ? book_loan.dateLoan|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_return</th>
                <td>{{ book_loan.dateReturn ? book_loan.dateReturn|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ book_loan.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('book_loan_index') }}">back to list</a>

    <a href="{{ path('book_loan_edit', {'id': book_loan.id}) }}">edit</a>

    {{ include('loan/_delete_form.html.twig') }}
{% endblock %}
